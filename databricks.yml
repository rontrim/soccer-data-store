# This is a Databricks asset bundle definition for soccer-data-store.
# The Databricks extension requires databricks.yml configuration file.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.

bundle:
  name: soccer-data-store

include:
  - resources/*.yml

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-3647422366695414.14.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-3647422366695414.14.azuredatabricks.net
    run_as:
      # This runs the job as the user deploying it, or you can specify a service principal
      user_name: rhetrim@gmail.com
